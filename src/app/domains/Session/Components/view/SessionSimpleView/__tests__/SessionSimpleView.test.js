import React from 'react'
import { render } from '@testing-library/react'
import SessionSimpleView from '../SessionSimpleView.template'

test('Render SessionSimpleView test.', () => {
  render(<SessionSimpleView />)
})

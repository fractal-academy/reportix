import ROUTES_PATHS from './routhPaths'
const ROLES = [
  // 'admin', 'user'
]

const START_PAGE = {
  USER: ROUTES_PATHS.DASHBOARD,
  ADMIN: ROUTES_PATHS.DASHBOARD
}
export default ROLES

export { START_PAGE }
